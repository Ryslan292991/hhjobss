"""Настройки. Секреты приходят из GitHub Secrets, остальное правится прямо здесь."""
import os

# --- Приходит из GitHub Secrets, руками не трогать ---
TELEGRAM_TOKEN = os.environ.get("TELEGRAM_TOKEN", "")
TELEGRAM_CHAT_ID = os.environ.get("TELEGRAM_CHAT_ID", "")
HH_USER_AGENT = os.environ.get("HH_USER_AGENT", "hh-jobs-bot/1.0 (mail@example.com)")
HH_TOKEN = os.environ.get("HH_TOKEN", "")  # нужен, только если hh начнёт отдавать 403

# --- Параметры поиска на hh ---
AREA = "113"                 # 113 = вся Россия
SCHEDULE = "remote"          # только удалёнка
EXPERIENCE = "noExperience"  # без опыта
PERIOD_DAYS = 3              # вакансии за последние N дней
MAX_PAGES = 3                # до 300 вакансий на один запрос

MAX_SEND_PER_RUN = 12        # потолок сообщений за прогон, чтобы не завалить чат
FORGET_AFTER_DAYS = 60       # через сколько дней забывать показанную вакансию

# Поисковые запросы. Добавляй и убирай строки свободно —
# это обычный текст, как в поисковой строке hh.
QUERIES = [
    # Контент, SMM, копирайтинг
    "копирайтер",
    "контент-менеджер",
    "SMM-менеджер",
    "редактор текстов",
    "автор статей",
    "контент-маркетолог",
    # Поддержка, ассистент, операционка
    "специалист технической поддержки",
    "ассистент руководителя удаленно",
    "личный помощник удаленно",
    "оператор поддержки пользователей",
    "администратор проекта",
    # Данные, аналитика, документация
    "технический писатель",
    "оператор ввода данных",
    "документовед",
    "младший аналитик",
    "асессор",
    "оператор баз данных",
]

# --- Пороги антифрод-фильтра ---
SCAM_DROP_THRESHOLD = 5   # >= этого — выбрасываем молча
SCAM_WARN_THRESHOLD = 3   # >= этого — присылаем с пометкой «проверь»
