"""Настройки. Секреты — из GitHub Secrets, остальное правится прямо здесь."""
import os

# --- Секреты (из GitHub Secrets) ---
TELEGRAM_TOKEN = os.environ.get("TELEGRAM_TOKEN", "")
TELEGRAM_CHAT_ID = os.environ.get("TELEGRAM_CHAT_ID", "")
HH_USER_AGENT = os.environ.get("HH_USER_AGENT", "jobs-bot/1.0 (mail@example.com)")
HH_TOKEN = os.environ.get("HH_TOKEN", "")  # опционально, если решишь чинить hh

# --- Какие источники включены ---
# telegram — публичные каналы с вакансиями, читаются через t.me/s/. Основной.
# trudvsem — выключен: в прошлый прогон дал 0 удалёнки. Включи, если захочешь
#            добавить «белые» госвакансии (иногда что-то попадается).
# hh — блокирует серверные IP GitHub (403). Включать только при запуске
#      со своего компьютера или российского VPS.
SOURCES = {
    "telegram": True,
    "trudvsem": False,
    "hh": False,
}

# --- Telegram-каналы ---
# Список без «@» и без «https://t.me/». Правь свободно.
# Как найти username: открой канал → в ссылке t.me/ИМЯ → впиши ИМЯ.
# Бот в логе пишет, сколько постов дал каждый канал, — пустые (0) удаляй.
TELEGRAM_CHANNELS = [
    # -- Контент, тексты, SMM, маркетинг --
    "textodromo",        # Текстодром — копирайтеры, редакторы (проверен, работает)
    "normrabota",        # Норм работа — контент, SMM (проверен, работает)
    "perezvonyu",        # Мы вам перезвоним — медиа и digital
    "reklamodromo",      # Рекламодром — маркетинг, реклама
    # -- Общая удалёнка, ассистенты, без опыта --
    "udalenka_vacansii", # Удалёнка — общий агрегатор, есть без опыта (проверен)
    "RabotaUdalenka",    # Работа из дома — свежая удалёнка
    "Well_paid_Job",     # Вакансии с з/п выше 50к — удалёнка из разных сфер
    "careerlift",        # CAREERLIFT — постоянка, фриланс, удалёнка
    "antirabstvoru",     # Антирабство — вакансии и стажировки, есть удалёнка
    # -- IT, digital, аналитика, поддержка, ассистенты --
    "it_vakansii_jobs",  # СЕТИ — IT & Digital, аналитика/ассистенты (проверен)
    "job4analyst_junior",# Аналитики без опыта
    "juno_jobs",         # Junior/intern вакансии и стажировки
]

# Ключевые слова релевантности: пост берётся, только если содержит хотя бы одно.
TELEGRAM_KEYWORDS = [
    # контент / тексты / маркетинг
    "копирайт", "редактор", "контент", "smm", "смм", "маркетолог",
    "автор статей", "тексты", "райтер", "переводчик",
    # поддержка / операционка / ассистент
    "поддержк", "техподдержк", "ассистент", "помощник", "администратор",
    "оператор чата",  # ловим — а антифрод решит, скам это или нет
    # данные / документы / аналитика
    "аналитик", "ввод данных", "оператор данных", "асессор", "разметк",
    "документовед", "модератор контента",
]

# --- Общие параметры поиска ---
PERIOD_DAYS = 3           # вакансии за последние N дней
MAX_SEND_PER_RUN = 15     # потолок сообщений за прогон
FORGET_AFTER_DAYS = 60    # через сколько дней забывать показанную вакансию

# Поисковые запросы. Общие для всех источников.
QUERIES = [
    # Контент, SMM, копирайтинг
    "копирайтер",
    "контент-менеджер",
    "SMM-менеджер",
    "редактор",
    "автор статей",
    # Поддержка, ассистент, операционка
    "оператор технической поддержки",
    "ассистент руководителя",
    "личный помощник",
    "администратор проекта",
    # Данные, аналитика, документация
    "технический писатель",
    "оператор ввода данных",
    "документовед",
    "младший аналитик",
    "асессор",
]

# --- hh: параметры (используются, только если включён hh) ---
HH_AREA = "113"                 # 113 = вся Россия
HH_SCHEDULE = "remote"          # удалёнка
HH_EXPERIENCE = "noExperience"  # без опыта
HH_MAX_PAGES = 2

# --- Trudvsem: параметры ---
# У госпортала фильтр по графику ненадёжен, поэтому удалёнку отбираем сами
# по тексту графика. Список того, что считаем удалёнкой:
TRUDVSEM_REMOTE_WORDS = ["дистанционн", "удалённ", "удаленн", "на дому"]
TRUDVSEM_MAX_PAGES = 3          # по 100 вакансий на запрос

# --- Пороги антифрод-фильтра ---
SCAM_DROP_THRESHOLD = 5   # >= этого — выбрасываем молча
SCAM_WARN_THRESHOLD = 3   # >= этого — присылаем с пометкой «проверь»
